@model Web_BanSach.NHAXUATBAN

@{
    <link href="~/Areas/Css/XoaLoai.css" rel="stylesheet" />
    ViewBag.Title = "ViewDelete";
}

<section class="delete-container">
    @using (Html.BeginForm("DeleteSubmit", "NhaXuatBan", new { area = "Admin" }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.MANXB)

        <div class="delete-card">
            <!-- Header -->
            <div class="delete-header">
                <div class="delete-icon">
                    <i class="fa-solid fa-trash-can"></i>
                </div>
            </div>

            <!-- Body -->
            <div class="delete-body">
                <div class="category-info">
                    <div class="category-label">Nhà xuất bản sẽ bị xóa</div>

                    <h3 class="category-name">
                        <i class="fa-solid fa-tag"></i>
                        @Model.TENNXB
                    </h3>

                    <div class="id-badge">
                        <i class="fa-solid fa-hashtag"></i>
                        Mã nhà xuất bản: @Model.MANXB
                    </div>
                </div>
            </div>

            <!-- Footer -->
            <div class="delete-footer">
                <a href="@Url.Action("Index", "NhaXuatBan", new { area = "Admin" })" class="btn-back">
                    <i class="fa-solid fa-arrow-left"></i>
                    Hủy bỏ
                </a>
                <button type="submit" class="btn-delete">
                    <i class="fa-solid fa-trash-can"></i>
                    Xóa
                </button>
            </div>
        </div>
    }
</section>